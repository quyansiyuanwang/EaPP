# RFC-NNNN　标题（一句话说清改什么）

| | |
|---|---|
| **状态** | Draft / Review / Accepted / Implemented / Rejected / Withdrawn |
| **§18.1 归类** | `3.0.x` / `3.x.0` / `4.0.0` |
| **协议版本影响** | 无 / 次版本 / 主版本，以及由此得到的版本号 |
| **评审** | 待评审 / 两名独立实现者 / 单实现（见 [`README.md`](./README.md) §3） |
| **作者** | |
| **日期** | |

---

## 1. 问题

现在的规范是怎么写的，问题在哪。

**规范实际留下了什么缺口。** 若两类实现可以各自理解这处文本、并都自称合规，
把两类理解都写出来。若不能给出这样的例子，问题可能不成立。

---

## 2. 提案

改后是什么。给出规范性措辞本身，而不只是描述它。

```text
XX-1  …
XX-2  …
```

---

## 3. 为什么不维持现状

不采纳会付出什么代价。给出**已经发生过**的具体情形；若只是"将来可能"，
说明为什么它一定会发生。

---

## 4. 对既有实现的影响

| 实现 | 会变成不合规吗 | 需要改什么 |
|---|---|---|
| `packages/`（TypeScript 参考实现） | | |
| `implementations/go/` | | |

---

## 5. 一致性

| 不变量 | 规则 | 测试 |
|---|---|---|
| `XX-1` | | |

`pnpm run check:invariants` 要求每条新不变量都对应一个测试 —— 在提案阶段就写出
测试的位置，避免落笔时才发现无从验证。

---

## 6. 被否决的替代方案

考虑过什么，为什么不选。**这一节比提案本身更能说明问题**：它记录了约束。

---

## 7. 未解决的问题

已知但未定的部分。留白好过含糊 —— 含糊会让两类实现各自猜一次。
