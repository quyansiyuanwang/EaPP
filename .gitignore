# ---- deps / build output ----
node_modules/
dist/
build/
*.tsbuildinfo
.vitest/
coverage/

# ---- editors / OS ----
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# ---- logs ----
*.log
npm-debug.log*
pnpm-debug.log*

# ---- local scratch ----
# NOTE: tmp/ is deliberately NOT ignored — tmp/draft/ holds the project's
# design drafts and is the provenance record for every frozen spec.
*.tmp
