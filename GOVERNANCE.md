# 治理

本文说明 EaPP 的版本策略、冻结与修订流程、决策记录方式，以及谁做什么决定。

---

## 1. 这个项目交付什么

**一份规范性正文**，外加**证据**。两者不是并列的：

```
规范        docs/spec/eapp.md     产品本身
证据        reference 分支        两份独立实现，与一个不属于任何一方的检查工具
```

一份不能被实现、也不能被验证的规范没有价值；一份没有规范约束的实现只是又一个框架。
因此本项目的"完成"定义是：

> **一个没有读过本仓库任何代码的第三方，只读 `docs/spec/eapp.md`，能够写出一份实现，
> 并据此为它的插件作者提供一套只经由 §51 的操作完成组合、互动与调用的表面。**

**证据为什么是两份实现。** 一份实现只能证明"这么写能跑通"。声明与实现出自同一支笔时，
两者之间的分歧要靠人来发现，而人恰好是写它们的那个人 —— 所以第二份实现由**没有读过第一份的人**、
只对着规范正文写出来。它存在的意义不是"多一个实现"，而是"规范正文本身是否足够写出一份实现"
这个问题的唯一诚实答案。跨实现检查工具不引用任何一方的代码，因此它检查的是协议的表面行为。

实现与检查工具**不放在主仓库**。一个同时装着产品与产品验证材料的仓库，会让读者把验证材料的
规模误当成交付物的规模，也会让"改实现比改规范便宜"这条捷径变得顺手。

---

## 2. 分卷与依赖方向

```
Composition Core   §4–§21     谁和谁组合
      │  单向依赖
      ▼
Interaction Layer  §22–§34    组合建立之后如何互动
      │
      ▼
State Mode         §35–§49    如何共享状态
      │
      ▼
Transport                     消息物理上怎么走
```

**下层 MUST NOT 反向定义上层语义。**

```
插件开发表面       §50–§54     五个操作组的统一形状
```

表面**不是第四层**。它不引入新的本体，只在形状上钉死前三部分已经要求过的操作。
任何一条表面规则与前三部分冲突时，以前三部分为准，且该冲突是表面的缺陷。

---

## 3. 版本策略

**协议版本是唯一的版本号**，写在 `docs/spec/eapp.md` 的元信息里。

| 版本类型 | 变更范围 |
|---|---|
| **3.0.x** | 勘误、文本澄清，不改变语义 |
| **3.x.0** | 新增不变量、新增合规等级、新增错误码、新增分卷或章节 |
| **4.0.0** | 修改任一既有不变量、修改分层方向、修改核心概念、修改操作语义 |

这三行是**规范 §2.1 本身的条款**，不是本文件的发明。当前协议版本 `3.5.0`。

**版本以标签锚定，不以文件名锚定。** 每次发布对应一个不可变的 git 标签 `spec-<协议版本>`。
规范正文的文件名是 `docs/spec/eapp.md`，不随版本推进而改变 —— 一个把版本写进文件名的规范，
会迫使读者在打开它之前先弄清楚文件名与当前版本的关系。

在此之前，三层各有一份独立冻结的文档（`v3.0.0-core` / `v3.1.0-interaction` / `v3.2.0-state`），
文件名是"冻结标识符"，与协议版本不是同一个号。那套制度带来两个代价：读者必须先读变更记录才能
知道当前版本；而同一套不变量标识符可以在两层里指不同的规则（`L-1` 曾是 Lifecycle，
也是 Lease）。合并为单一正文后两者都不存在。旧文本仍在标签 `spec-3.3.0` 上可达。

---

## 4. 冻结与修订

```
docs/spec/eapp.md         规则是什么。冻结正文，就地勘误
CHANGELOG.md              协议版本之间改了什么、为什么
docs/rationale.md         每条规则为什么是这样（非规范性）
rfcs/                     改变语义的提案，及其评审状态
```

**勘误就地写入正文，并在 `CHANGELOG.md` 留一条。**

**正文 MUST 单独可读。** 规范文本 MUST NOT 依赖任何 `docs/spec/` 之外的文件，
也 MUST NOT 链接到外部。一处曾经的做法值得记下来，因为它已经变成了一条规则：早期的分层文档
把勘误表放进规范正文，使"规范文本"等于"另一份文档经逐条替换后的结果"；那份文档随后被移出工作树，
规范于是不再自足。`check:spec` 现在把这条规则变成了判定。

设计依据**不是**规范的一部分，但必须保留：规范只陈述规则是什么，不说明它为什么不是别的样子。
下一个维护者若只看规则，会把同样的错误再犯一遍。

---

## 5. 修订流程

```
提议             issue，说明要解决什么问题（不是要加什么功能）
   ↓
设计             改规范正文；写清楚它替换了什么、为什么
   ↓
独立评审          由未参与设计的人逐条核对：内部矛盾、未定义符号、跨分卷冲突
   ↓
决议记录          每条裁定入库，含理由
   ↓
一致性           每条不变量在实现方有一个测试（规范 §3.1）
   ↓
打标签            不可变锚点
```

**评审必须是独立的。** 一次自称"6 个 P0 与 6 个 P1 全部修正"的设计，独立评审发现实际还剩 40 条。
自评在这个项目里被证明是不可靠的。

**未能取得评审时，把缺口写出来，而不是默认为通过。** 规范正文的元信息里有一行状态，
`rfcs/` 的状态表里有一列 —— 两处都必须反映同一个事实。已知限制要可见，不能让流程被静默绕过。

---

## 6. 决策记录

| 文件 | 记录什么 |
|---|---|
| `docs/rationale.md` | 每条规则为什么不是别的样子 |
| `rfcs/` | 改变语义的提案，及其评审状态与被否决的替代方案 |
| `CHANGELOG.md` | 协议版本之间的全部修订 |
| `docs/spec/eapp.md` §3.2 | 一致性声明的字段与 `levels` 的取值域 |

判断一条规则时，先读规范，再读 `docs/rationale.md`：规范定义规则，依据说明它为什么不是别的样子。

---

## 7. 偏离规范

实现允许偏离规范，但**必须登记**，且必须给出理由。未登记的偏离视为缺陷。

偏离有两种，处理方式不同：

| 情形 | 处理 |
|---|---|
| 规范自身矛盾，实现选了其中一种读法 | 改规范。正文修订后，该偏离消失 |
| 规范留白，实现补了一个选择 | 先判断这是不是形状问题 —— 若是，它属于表面（§50），应补进规范；若不是，登记为已知限制 |

**"实现与规范冲突时先确认哪一边错了"**，这条规则的价值在于它把留白暴露出来。
本项目已经出现过多次：规范把语义留白，两份实现各自补一个合理的选择，都自称合规。
那类修正往往比补一个 bug 更有价值。

---

## 8. 角色

当前为单一维护者模式。随着贡献者增加，需要的角色是：

| 角色 | 职责 |
|---|---|
| **Maintainer** | 合并、发布、打标签 |
| **Spec Editor** | 维护规范正文、变更记录与设计依据之间的一致性 |
| **Reviewer** | 独立评审；由未参与该设计的人担任 |
| **Implementer** | 维护一份实现，并参与规范变更的评审 |

**独立评审是本项目唯一不可省略的角色。** 见 §5。
评审者不必是本仓库的贡献者：任何维护一份 EaPP 实现的人都是合格的评审者，
因为规范变更的代价由实现方承担。

---

## 9. 一致性声明

任何实现都可以自称实现 EaPP。声明在规范 §3.2 中定义：

```
eappVersion   实现所覆盖到的协议版本
levels        已声明的合规等级，取值必须出自规范定义过的等级
testSuite     所用一致性测试套件的标识与版本
passed/total  通过的测试用例数与总数
```

`levels` 里**只允许列规范真正定义过的等级**：第 I 部分定义 `C1`–`C8`（§17），
第 II 部分定义 `I1`–`I7`（§34），第 IV 部分定义 `CS1`–`CS5`（§53）。
State Mode 不定义等级前缀，它的覆盖度由不变量计数表达。

声明一个规范没有定义过的等级不是扩展，是伪造合规：跨实现互通靠的就是这些名字，
任何实现都可以自己发明名字，就等于谁都不认这些名字。

本协议**不设认证机构**。声明由实现方作出，由任何愿意运行黑盒检查的人验证 ——
`reference` 分支上的 driver 协议与检查工具是为此准备的一套装置，但它不属于任何一方，
也不构成对声明的背书。
