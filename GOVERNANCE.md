# 治理

本文说明 EaPP 的版本策略、冻结与勘误流程、决策记录方式，以及谁做什么决定。

---

## 1. 这个项目交付什么

**一份协议规范**，外加它的**参考实现**与**一致性套件**。三者不是并列的：

```
规范        docs/spec/            产品本身
参考实现    packages/             证明规范可实现
一致性套件  tests/conformance/    证明实现符合规范
```

一份不能被实现、也不能被验证的规范没有价值；一份没有规范约束的实现只是又一个框架。
所以本项目的"完成"定义是：**规范冻结 + 一致性全绿**。

---

## 2. 层次与依赖方向

```
Composition Core  (v3.0)   谁和谁组合
      │  单向依赖
      ▼
Interaction Layer (v3.1)   组合后如何互动
      │
      ▼
State Mode        (v3.2)   如何共享状态
      │
      ▼
Transport                  消息物理上怎么走
```

**下层 MUST NOT 反向定义上层语义。**

State Mode 是 Interaction Layer 的**第四种 mode，不是新层** ——
它复用 `Channel` / `Subscription` / `Cursor` / `AckContext` / `Transport`，
只新增四个本体。

---

## 3. 版本策略

```
v3.0.0-core            FROZEN
v3.1.0-interaction     FROZEN
v3.2.0-state           FROZEN
v4.0.0-*               未来重构
```

### 3.x 内允许

```
新增不变量（向后兼容扩展）
新增合规等级
新增错误码（不改既有语义）
勘误（澄清歧义，不改变语义）
新增 Extension 章节（不污染 Core）
```

### 3.x 内不允许（须进入 4.0）

```
修改任一既有不变量
修改 Binding 派生规则
修改 Lifecycle 状态转移
修改 Identity 结构
修改 CapabilityRef 语义
修改层级方向
```

这三份清单直接取自 v3.0 §0，是**冻结条款本身**，不是本文件的发明。

---

## 4. 三层各自怎么"冻结"

三层的成熟路径不同，冻结方式也不同。这是有意的。

### v3.0 —— 自发布起冻结

一次性冻结。修改走 §3 的清单。**不允许**重写正文。

### v3.1 —— 草案正文 + 勘误表

v3.1 冻结时是**草案**（文末自述 `Draft`），冻结的方式是
「正文 + 一份规范性勘误表」，而不是重写正文：

```
文档 = 草案正文，经 §1 勘误表逐条替换后的结果
条目形如 E1-n：原文 → 替换为 → 理由
```

好处是**改动可审计**：任何一条修订都能看到它替换了什么、为什么。

### v3.2 —— 决议记录 + 重写正文

v3.2 的 r2 有 40 条内部缺陷与 12 处跨文档冲突，逐条打补丁已经不成立，
因此采用「**决议记录 + 重写正文**」：

```
docs/spec/DECISIONS-v3.2.0-r3.md   每条规则为什么是这样
docs/spec/v3.2.0-state.md          规则是什么
```

决议记录**不是**规范的一部分，但它必须保留 —— 否则下一个维护者会
把同样的错误再犯一遍。r2 的 `snapshot().maxRevision` 用 `''` 作归约种子，
正是因为没有人记得当初为什么要那样写。

---

## 5. 冻结与勘误流程

```
提议                  issue，说明要解决什么问题（不是要加什么功能）
   ↓
设计                  改规范草案；写清楚它替换了什么、为什么
   ↓
独立评审              由未参与设计的人逐条核对：内部矛盾、未定义符号、跨文档冲突
   ↓
决议记录              每条裁定入库，含理由与被关闭的缺陷编号
   ↓
实现 + 一致性测试     每条不变量一个测试（v3.0 §19.2）
   ↓
冻结闸门              差集非空即失败
   ↓
打标签                不可变锚点
```

**评审必须是独立的。** v3.2 的 r2 自称"6 P0 + 6 P1 全部修正"，
独立评审发现实际还剩 40 条。自评在这个项目里被证明是不可靠的。

---

## 6. 决策记录

| 文件 | 记录什么 |
|---|---|
| `docs/spec/DECISIONS-v3.2.0-r3.md` | 51 条裁定的**理由**（D-01…D-38 内部，R-0…R-13 跨文档） |
| `docs/analysis/GAP-ANALYSIS-v3.2.0-r2.md` | 缺陷清单（F-01…F-39）与跨文档冲突（X-1…X-12） |
| `docs/CONFORMANCE.md` §5 | 实现相对规范的**已登记偏离** |
| `docs/spec/CHANGELOG.md` | 版本之间的全部修订与映射 |

判断一条规则时，**先读决议记录**。规范告诉你规则是什么，决议记录告诉你为什么。

---

## 7. 偏离规范

实现允许偏离规范，但**必须登记**，且必须给出理由。
未登记的偏离视为缺陷。

`docs/CONFORMANCE.md` §5 记录了当前 4 条，例如：

> `StateChannel.watch()` 返回 `Promise<StateWatcher>`。原规范声明它同步，
> 但又要求初始 cursor 是具体位置。解析 `'latest'` 需要异步读 head，
> 同步形式永远无法满足规范自己的 `SUB-9`。

这条偏离**修正了规范**，而不是绕开它 —— 规范正文已同步修订。

---

## 8. 角色

当前为单一维护者模式。随着贡献者增加，需要的角色是：

| 角色 | 职责 |
|---|---|
| **Maintainer** | 合并、发布、冻结标签 |
| **Spec Editor** | 维护规范正文与决议记录的一致性 |
| **Reviewer** | 独立评审；由未参与该设计的人担任 |

**独立评审是本项目唯一不可省略的角色。** 见 §5。

---

## 9. 不合规的声明

任何实现都可以自称实现 EaPP。**本项目只对自己的参考实现做一致性声明**，
声明内容与证据见 `docs/CONFORMANCE.md`：

```json
{ "eappVersion": "3.2.0", "levels": ["C1", "…", "I6", "S1"],
  "testSuite": "conformance@3.2.0-r3", "passed": 208, "total": 208 }
```

第三方实现的合规等级由它自己声明 —— 本协议不设认证机构，
这与"它是一个协议，不是一个产品"是一致的。
