# 治理

本文说明 EaPP 的版本策略、冻结与勘误流程、决策记录方式，以及谁做什么决定。

---

## 1. 这个项目交付什么

**一份协议规范**，外加**两份独立实现**与**一个不属于任何一方的检查工具**。
它们不是并列的：

```
规范        docs/spec/            产品本身
参考实现    packages/             证明规范可实现（TypeScript，三层完整）
独立实现    implementations/      证明规范可被**别人**实现（Go，Composition Core）
一致性套件  tests/conformance/    证明参考实现符合规范
外部 harness conformance/         证明**任意**实现符合规范（黑盒，语言中立）
```

一份不能被实现、也不能被验证的规范没有价值；一份没有规范约束的实现只是又一个框架。
所以本项目的"完成"定义是：**规范冻结 + 一致性全绿 + 至少两份独立实现过同一批外部检查**。

**为什么是两份。** 一份实现只能证明"这么写能跑通"。声明与实现出自同一支笔时，
两者之间的分歧要靠人来发现，而人恰好是写它们的那个人 —— 所以第二份实现由
**没有读过第一份的人**、只对着规范正文写出来。它存在的意义不是"多一个实现"，
而是"规范正文本身是否足够写出一份实现"这个问题的唯一诚实答案。

---

## 2. 层次与依赖方向

```
Composition Core  (v3.0)   谁和谁组合
      │  单向依赖
      ▼
Interaction Layer (v3.1)   组合后如何互动
      │
      ▼
State Mode        (v3.2)   如何共享状态
      │
      ▼
Transport                  消息物理上怎么走
```

**下层 MUST NOT 反向定义上层语义。**

State Mode 是 Interaction Layer 的**第四种 mode，不是新层** ——
它复用 `Channel` / `Subscription` / `Cursor` / `AckContext` / `Transport`，
只新增四个本体。

---

## 3. 版本策略

```
v3.0.0-core            FROZEN
v3.1.0-interaction     FROZEN
v3.2.0-state           FROZEN
v4.0.0-*               未来重构
```

### 3.x 内允许

```
新增不变量（向后兼容扩展）
新增合规等级
新增错误码（不改既有语义）
勘误（澄清歧义，不改变语义）
新增 Extension 章节（不污染 Core）
```

### 3.x 内不允许（须进入 4.0）

```
修改任一既有不变量
修改 Binding 派生规则
修改 Lifecycle 状态转移
修改 Identity 结构
修改 CapabilityRef 语义
修改层级方向
```

这三份清单直接取自 v3.0 §0，是**冻结条款本身**，不是本文件的发明。

---

## 4. 三层各自怎么"冻结"

三层的成熟路径不同，冻结方式也不同。这是有意的。

### 冻结与修订机制

三层使用同一机制：

```
docs/spec/v3.x.0-*.md           规则是什么。冻结文本，就地勘误，不重写
docs/spec/CHANGELOG.md          改了什么、为什么
docs/analysis/DECISIONS-*.md    每条规则为什么是这样（非规范性）
```

**勘误就地写入正文，并在 `docs/spec/CHANGELOG.md` 留一条。**

一处曾经的做法值得记下来，因为它已经变成了一条规则：v3.1 冻结时把勘误表
放进规范正文，使"规范文本"等于"另一份文档经逐条替换后的结果"。那份文档随后
被移出工作树，规范于是不再自足。**冻结文本必须能单独读懂** ——
正文 MUST NOT 依赖任何不在 `docs/spec/` 内的文件。

决议记录**不是**规范的一部分，但必须保留：否则下一个维护者会把同样的错误
再犯一遍。规范只陈述规则是什么，不说明它为什么不是别的样子。

---

## 5. 冻结与勘误流程

```
提议                  issue，说明要解决什么问题（不是要加什么功能）
   ↓
设计                  改规范正文；写清楚它替换了什么、为什么
   ↓
独立评审              由未参与设计的人逐条核对：内部矛盾、未定义符号、跨文档冲突
   ↓
决议记录              每条裁定入库，含理由与被关闭的缺陷编号
   ↓
实现 + 一致性测试     每条不变量一个测试（v3.0 §19.2）
   ↓
冻结闸门              差集非空即失败
   ↓
打标签                不可变锚点
```

**评审必须是独立的。** v3.2 的 r2 自称"6 P0 + 6 P1 全部修正"，
独立评审发现实际还剩 40 条。自评在这个项目里被证明是不可靠的。

---

## 6. 决策记录

| 文件 | 记录什么 |
|---|---|
| `docs/analysis/DECISIONS-v3.2.0-r3.md` | 51 条裁定的**理由**（D-01…D-38 内部，R-0…R-13 跨文档） |
| `docs/CONFORMANCE.md` §5 | 实现相对规范的**已登记偏离** |
| `docs/spec/CHANGELOG.md` | 版本之间的全部修订与映射 |

判断一条规则时，**先读决议记录**。规范定义规则，决议记录说明其依据。

---

## 7. 偏离规范

实现允许偏离规范，但**必须登记**，且必须给出理由。
未登记的偏离视为缺陷。

`docs/CONFORMANCE.md` §5 记录了当前 4 条，例如：

> `StateChannel.watch()` 返回 `Promise<StateWatcher>`。原规范声明它同步，
> 但又要求初始 cursor 是具体位置。解析 `'latest'` 需要异步读 head，
> 同步形式永远无法满足规范自己的 `SUB-9`。

这条偏离**修正了规范**，而不是绕开它 —— 规范正文已同步修订。

---

## 8. 角色

当前为单一维护者模式。随着贡献者增加，需要的角色是：

| 角色 | 职责 |
|---|---|
| **Maintainer** | 合并、发布、冻结标签 |
| **Spec Editor** | 维护规范正文与决议记录的一致性 |
| **Reviewer** | 独立评审；由未参与该设计的人担任 |

**独立评审是本项目唯一不可省略的角色。** 见 §5。

---

## 9. 一致性声明

任何实现都可以自称实现 EaPP。本仓库里有两份实现，各自声明自己的覆盖：

```json
{ "eappVersion": "3.2.0", "levels": ["C1", "…", "I6", "I7"],
  "testSuite": "conformance@3.2.0-r3", "passed": 209, "total": 209 }
```

`levels` 里**只允许列规范真正定义过的等级** —— v3.0 §15 定义 `C1`–`C8`，
v3.1 §15 定义 `I1`–`I7`。声明一个规范没定义过的等级（例如本项目早期笔下的
`"S1"`）不是扩展，是伪造合规：跨实现互通靠的就是这些名字，
谁都可以自己发明就等于谁都不认。

第三方的合规等级由它自己声明，但**不再只能靠自称**：

```bash
pnpm run conformance:external
```

`conformance/` 里的 harness 是一个不属于任何实现的检查工具 —— 它按
[driver 协议](conformance/driver.md) 拉起一个可执行文件，只看它的 JSON 回答。
本协议仍然不设认证机构，但"声明应当可验证"从这句话变成了一条命令。
覆盖到哪里、哪里没覆盖，逐条写在 [`conformance/README.md`](conformance/README.md) 里。
